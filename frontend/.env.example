# Browser-visible API base for the FastAPI control plane.
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Comma-separated dev origins allowed by Next.js dev server.
NEXT_ALLOWED_DEV_ORIGINS=localhost,127.0.0.1
